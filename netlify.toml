[build]
  command = "npm run build"
  publish = "dist"

# React Router (SPA): serve index.html for all routes so /admin, /koreads, etc. work on refresh and direct links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
